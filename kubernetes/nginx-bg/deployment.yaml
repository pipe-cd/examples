apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-bg
  labels:
    app: nginx-bg
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-bg
      pipecd.dev/variant: primary
  template:
    metadata:
      labels:
        app: nginx-bg
        pipecd.dev/variant: primary
    spec:
      containers:
      - name: nginx-bg
        image: nginx:latest
        args:
          - server
        ports:
        - containerPort: 80
